 <div class="content-page tabcontent" id="tabAuth">
            <div class="content-code"></div>
            <div class="content">
                <div class="overflow-hidden content-section">
                    <h1>Auth Transacsions</h1>
                    <p>The user performs the login, sign up and account delete operations with the following examples.</p>
                </div>
                <div class="overflow-hidden content-section">
                    <h2>LOGIN</h2>
                    <pre><code>
#Request Example
POST <url id="path">login</url>
{
    "UserIdentityNo": 1,
    "UserPassword": "password"
}       
Result Example:
{
    "result": {
        "UserID": 15,
        "UserFirstName": "STA",
        "UserLastName": "PROJECT",
        "UserPassword": "password",
        "UserIdentityNo": 1,
        "UserEmail": "info@sta.com",
        "UserPhone": "05555555555",
        "InstitutionID": 1,
        "UserStatusID": 1
    },
    "token": "SECRET-TOKEN"
}</code></pre>
                    <p>For the user login, you need to make a POST call to the URL below : 
                        <br>
                        <code class="higlighted" id="path">login</code>
                    </p>
                </div>
                <div class="overflow-hidden content-section">
                    <h2>Sign Up</h2>
                    <pre><code>
#Request Example
POST <url id="path">sign-up</url>
{
    "UserID": 15,
    "UserFirstName": "STA",
    "UserLastName": "PROJECT",
    "UserPassword": "password",
    "UserIdentityNo": 1,
    "UserEmail": "info@sta.com",
    "UserPhone": "05555555555",
    "InstitutionID": 1,
    "UserStatusID": 1
}
        
Reusult Example:
{
    message: "User registration has taken place."
}</code></pre>
                    <p>For the sign up transaction, you need to make a POST call to the URL below. User must login for this operation.: 
                        <br>
                        <code class="higlighted" id="path">sign-up</code>
                    </p>
                </div>
                <div class="overflow-hidden content-section">
                    <h2>My Account Delete</h2>
                    <pre><code>
#Request Example
DELETE <url id="path">my-account-delete</url>      

Reusult Example:
{
    "message": "Deletion succeeded."
}</code></pre>
                    <p>For the delete my account transaction, you need to make a DELETE call to the URL below. User must login for this operation.: 
                        <br>
                        <code class="higlighted" id="path">my-account-delete</code>
                    </p>
                </div>
                <div class="overflow-hidden content-section">
                    <h2>Token Decode</h2>
                    <pre><code>
#Request Example
GET <url id="path">token-decode</url>

        
Reusult Example:
{
    "UserID": 15,
    "UserStatusID": 1,
    "InstitutionID": 12,
    "iat": 1600884627,
    "exp": 1631988627
}</code></pre>
                    <p>For the token decode, you need to make a GET call to the URL below. User must login for this operation.:
                        <br>
                        <code class="higlighted" id="path">token-decode</code>
                    </p>
                </div>
            </div>
            <div class="content-code"></div>
        </div>